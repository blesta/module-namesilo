    <div class="title_row first">
        <h3><?php $this->_('Dynadot.tab_whois.title');?></h3>
    </div>
    <div class="pad">
        <?php
        $this->Form->create();
        ?>
        <?php
        $sections = ['Registrant', 'Admin', 'Technical', 'Billing'];
        foreach ($sections as $section) {
            $section_key = strtolower($section);
        ?>
            <h4><?php $this->_('Dynadot.tab_whois.section_' . $section_key);?></h4>
            <ul>
                <?php
                foreach ($fields as $key => $field) {
                    if (strpos($key, $section) !== 0) {
                        continue;
                    }
                    ?>
                    <li>
                        <?php
                        $this->Form->label($field['label'], $key);
                        $this->Form->fieldText($key, (isset($vars->{$key}) ? $vars->{$key} : null), ['id' => $key, 'class' => 'block']);
                        ?>
                    </li>
                    <?php
                }
                ?>
            </ul>
        <?php
        }
        ?>
    </div>
    <div class="button_row">
        <?php
        $this->Form->fieldSubmit('save', $this->_('Dynadot.tab_whois.field_submit', true), ['class' => 'btn btn-primary pull-right']);
        ?>
    </div>
    <?php
    $this->Form->end();
    ?>
